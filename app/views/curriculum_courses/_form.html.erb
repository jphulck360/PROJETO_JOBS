<%= form_for(@curriculum_course, html: { class: "form-horizontal", role: "form" }) do |f| %>
  <% if @curriculum_course.errors.any? %>
    <div class="alert alert-danger alert-dismissable" role="alert">
      <button type="button" class="close" data-dismiss="alert"><span aria-hidden="true">&times;</span><span class="sr-only">Close</span></button>
      <h4><%= pluralize(@curriculum_course.errors.count, "error") %> prohibited this curriculum_course from being saved:</h4>

      <ul>
      <% @curriculum_course.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <div class="col-sm-offset-2 col-sm-10">
      <%= f.submit class: "btn btn-primary" %>
    </div>
  </div>
<% end %>
